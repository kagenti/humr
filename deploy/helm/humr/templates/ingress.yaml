{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "humr.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "humr.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  rules:
    - host: onecli.localtest.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "humr.onecli.fullname" . }}
                port:
                  name: web
    - host: onecli.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "humr.onecli.fullname" . }}
                port:
                  name: web
    - host: humr.localtest.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ printf "%s-ui" (include "humr.fullname" .) | trunc 63 | trimSuffix "-" }}
                port:
                  name: http
    - host: humr.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ printf "%s-ui" (include "humr.fullname" .) | trunc 63 | trimSuffix "-" }}
                port:
                  name: http
    - host: humr-api.localtest.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ printf "%s-apiserver" (include "humr.fullname" .) | trunc 63 | trimSuffix "-" }}
                port:
                  name: http
    - host: humr-api.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ printf "%s-apiserver" (include "humr.fullname" .) | trunc 63 | trimSuffix "-" }}
                port:
                  name: http
{{- end }}
