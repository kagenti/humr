# DRAFT: UI wireframes and tooling

**Date:** 2026-04-02
**Status:** Proposed
**Owner:** @PetrBulanek

## Context

The prototype needs a user interface. The team has debated code-driven vs. chat-driven agent development (still open). The UI decision affects how developers interact with the platform: do they get a chat interface (OpenClaw-style), a dashboard (ADK-style), a CLI, or some combination?

Related tensions: Tomas W. wants chat-driven everything. Matous wants code control for complex agents. The consumption experience (how users see agent results) is also unresolved.

## Decision

TBD. Questions to answer:

- What is the primary interaction model? Chat? Dashboard? CLI?
- What tooling do we use for wireframes/prototyping? (Figma, code-first, etc.)
- Do we build a custom UI or extend an existing one (e.g., OpenClaw's UI, ADK's web UI)?
- What's the minimum UI for the prototype vs. what can wait?

## Alternatives Considered

TBD — needs team discussion.

## Consequences

TBD — depends on the decision.
