# Review #002

<!-- tseng_version: 2.0.0 -->
<!-- status: locked -->
<!-- created: 2026-04-14 -->
<!-- locked: 2026-04-14 -->
<!-- based_on: #001 -->

Generated from architecture docs (tseng v2.0.0).
Upgrade from review [#001](001.md) (tseng v1.0.0).

## New Rules

- [x] RxJS (recommended) for reactive event bus for domain events and sagas
- [x] Infrastructure layer (infrastructure/) implements ports defined by inner layers
- [x] Infrastructure may import from domain but never from services
- [x] Repository pattern — services depend on repository interface (port), not storage implementation
- [x] Repository interfaces live in infrastructure/ as type definitions
- [x] Concrete repository is a factory function returning the interface
- [x] Mappers are pure functions translating between infrastructure and domain representations
- [x] Mappers live in infrastructure/ alongside repository implementations
- [x] External system adapters implement port interfaces
- [x] Services receive dependencies via factory functions (DI without container)
- [x] Each module has compose.ts wiring infrastructure to services
- [x] Composition root is the only place where concrete implementations are referenced
- [x] Module definition (contract package) vs module implementation (server package) distinction
- [x] Contract package has flat module structure: types.ts + router.ts per module
- [x] Contract router accepts service implementation and delegates — no business logic
- [x] Server package implements service interfaces defined in contract package
- [x] Event bus with central event registry (enum + union type) in events.ts at server package root
- [x] emit() for publishing, events$() + ofType<T>() for subscribing
- [x] Sagas (process managers) in sagas/ directory within the owning module
- [x] Sagas return subscription handles for teardown
- [x] Sagas receive dependencies via injection
- [x] tseng/project-structure.md with machine-readable package metadata
- [ ] Server packages require rxjs as dependency

## Changed Rules

- [x] Validation layer lives in contract package, not in server modules (was: "Validation layer (routers/) uses tRPC + Zod, no business logic")
- [x] Each module has services/, domain/, infrastructure/, sagas/(optional), compose.ts, index.ts (was: "Each module has routers/, services/, domain/, index.ts structure")

## Unchanged (carried forward from #001)

- [x] Uses tRPC for API layer
- [x] Uses Zod for input validation
- [x] TypeScript strict mode enabled (tsconfig.base.json)
- [x] pnpm for package management with workspace support
- [x] Server runtime is Hono or Express with tRPC adapter
- [x] tRPC router mounted on server
- [x] Client uses tRPC client for type-safe API calls
- [x] API contract package exports AppRouter type
- [x] Dev proxy or CORS configured for development
- [x] Three-layer architecture: validation → services → domain
- [x] Application layer (services/) orchestrates use cases
- [x] Domain layer (domain/) is pure TypeScript, zero external deps
- [x] Dependencies flow inward only: services → domain ← infrastructure
- [ ] Domain errors represented as Result<T, E> values, not thrown exceptions
- [x] Services never import from validation layer
- [x] Domain layer never imports from other layers
- [x] Server organized into modules under src/modules/
- [x] Each module is a vertical slice (bounded context)
- [x] Modules defined by business boundaries, not technical concerns
- [x] Module index.ts exports only domain event types and type guards
- [x] Domain events in domain/events/ directory
- [x] No cross-module imports except event types from index.ts
- [x] Inter-module communication via domain events only
- [x] Events named in past tense using business language
- [x] Events have type discriminant and type guard
- [x] Publishing module owns event definitions
- [x] Monorepo with workspace config (pnpm-workspace.yaml or package.json workspaces)
- [x] Server packages have @trpc/server and zod as dependencies
- [x] Client packages have @trpc/client as dependency
- [x] tsconfig.base.json at root with strict settings
- [x] Each package tsconfig extends root tsconfig.base.json

## Removed Rules (informational)

(none)
